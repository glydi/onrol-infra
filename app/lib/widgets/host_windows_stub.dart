// Non-web fallback: opening side-by-side browser windows is a web-only trick.
// On mobile this is a no-op (the host uses the in-app tabbed panel instead).
void openStudioAndAnswers(String studioUrl, String answersUrl) {}
